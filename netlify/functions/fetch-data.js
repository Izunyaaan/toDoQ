export async function handler(event, context) {
    try {
        const firebaseApiKey = process.env.FIREBASE_API_KEY;
        // insert rest of logic here later once firebase is setup
    }
    catch (error) {
        console.error(error);
    }
}